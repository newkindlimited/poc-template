/**
 * Dashboard placeholder. Claude Code replaces this with the real
 * dashboard for the target — KPI tiles, recent activity, etc.
 * computed from seed data so it feels like a real working business.
 */
export default function Dashboard() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="text-muted-foreground mt-2">
        Claude Code will replace this with the target&apos;s real dashboard.
      </p>
    </main>
  );
}
